language: ruby

cache:
  bundler: true
  directories:
    - vendor/bundler

before_install:
  - make doctor || echo "Some system dependencies might be missing"

install:
  - make install

script:
  - make check
  - make test

notifications:
  email:
    on_success: never
    on_failure: change
